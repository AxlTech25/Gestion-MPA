# Copie a .env.production antes de compilar para Hostinger
# Ruta de la API relativa al dominio (raíz del sitio)
VITE_API_BASE_URL=/backend/api/v2

# Ruta base del frontend (use / si el sitio está en la raíz del dominio)
VITE_BASE_PATH=/
